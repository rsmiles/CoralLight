# Genera of Coral Surveyed

# Generate a pie chart of all coral genera at specified sites.

@input site fieldlist sheet site_name
@chart pie
SELECT genus, COUNT(*)
FROM data
WHERE TRIM(site_name) IN $site
GROUP BY genus;

