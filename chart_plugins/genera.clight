# Genera of Coral Surveyed

# Generate a pie chart of all coral genera at specified sites.

@input site field site_name
@chart pie
SELECT genus, COUNT(*)
FROM data
WHERE TRIM(site_name) = $site
GROUP BY genus;
